// CAPTCHA verification was removed. This stub prevents crashes if any
// lingering import still references this module.
async function verifyCaptcha() {
  return true;
}

module.exports = { verifyCaptcha };
